name: Probe workflow-run
on:
  workflow_run:
    workflows: [Mock Gates]
    types: [completed]
permissions: {contents: read}
jobs:
  p:
    if: false
    runs-on: ubuntu-latest
    steps: [{run: "echo probe"}]
